// Демонстрирует пошаговое построение ошибки через fluent-интерфейс.
// Сценарий выводит тип построенной ошибки, данные и подсказку.

#Использовать errors

Ошибка = ФабрикаОшибок.Конструктор("http.proxy_error", "Не удалось подключиться через прокси")
	.Данные(Новый Структура("Адрес", "proxy.example.org:3128"))
	.Подсказка("Проверьте адрес прокси и сетевое подключение")
	.Построить();

Исследователь = Новый ИсследовательОшибки(Ошибка);
Данные = Исследователь.Данные();

Сообщить("Тип: " + Исследователь.ТипОшибки());
Сообщить("Прокси: " + Данные.Адрес);
Сообщить("Подсказка: " + Исследователь.Подсказка());
