#Использовать asserts
#Использовать "fixtures"
#Использовать "../src"

#Область Тесты

&Тест
Процедура ТестДолжен_ВернутьКраткоеПредставлениеСмешаннойЦепочки() Экспорт

	// Подготовка
	Цепочка = СмешаннаяЦепочка();

	// Действие
	Представление = (Новый ИсследовательОшибки(Цепочка.Ошибка)).КраткоеПредставление();

	// Проверка
	Ожидаем.Что(Представление).Равно(
		"config.load_failed: Загрузка конфигурации -> Не удалось загрузить конфигурацию"
	);

КонецПроцедуры

&Тест
Процедура ТестДолжен_ВернутьСообщениеВнешнегоКадраВКраткомПредставлении() Экспорт

	// Подготовка
	Ошибка = ПомощникТестированияОшибок.ВнешняяОшибка(
		"В файле конфигурации обнаружен некорректный JSON"
	);

	// Действие
	Представление = (Новый ИсследовательОшибки(Ошибка)).КраткоеПредставление();

	// Проверка
	Ожидаем.Что(Представление).Равно("В файле конфигурации обнаружен некорректный JSON");

КонецПроцедуры

&Тест
Процедура ТестДолжен_ВернутьПустоеСообщениеПервогоВнешнегоКадра() Экспорт

	// Подготовка
	Корень = ПомощникТестированияОшибок.ВнешняяОшибка("Корневая ошибка");
	Ошибка = ПомощникТестированияОшибок.ВнешняяОшибка("", Корень);

	// Действие
	Представление = (Новый ИсследовательОшибки(Ошибка)).КраткоеПредставление();

	// Проверка
	Ожидаем.Что(Представление).Равно("");

КонецПроцедуры

&Тест
Процедура ТестДолжен_СохранитьПереводыСтрокВКраткомПредставлении() Экспорт

	// Подготовка
	Ошибка = ФабрикаОшибок.Создать("core.failed", "Первая причина" + Символы.ПС + "Вторая причина");
	Ошибка = ФабрикаОшибок.СКонтекстом(
		Ошибка,
		"Первый контекст" + Символы.ПС + "Второй контекст"
	);

	// Действие
	Представление = (Новый ИсследовательОшибки(Ошибка)).КраткоеПредставление();

	// Проверка
	Ожидаем.Что(Представление).Равно(
		"core.failed: Первый контекст" + Символы.ПС + "Второй контекст -> Первая причина"
			+ Символы.ПС + "Вторая причина"
	);

КонецПроцедуры

&Тест
Процедура ТестДолжен_ВернутьПервыйКонтекстЦепочкиБезКлассифицированнойОшибки() Экспорт

	// Подготовка
	Причина = ПомощникТестированияОшибок.ВнешняяОшибка("Ошибка сторонней библиотеки");
	Ошибка = ФабрикаОшибок.СКонтекстом(Причина, "Запуск приложения");

	// Действие
	Представление = (Новый ИсследовательОшибки(Ошибка)).КраткоеПредставление();

	// Проверка
	Ожидаем.Что(Представление).Равно("Запуск приложения");

КонецПроцедуры

&Тест
Процедура ТестДолжен_ВернутьПодробноеПредставлениеВсехКадров() Экспорт

	// Подготовка
	Цепочка = СмешаннаяЦепочка();

	// Действие
	Представление = (Новый ИсследовательОшибки(Цепочка.Ошибка)).ПодробноеПредставление();

	// Проверка
	ОжидаемоеПредставление = "config.load_failed: Не удалось загрузить конфигурацию
	|
	|Контекст:
	|  Загрузка конфигурации
	|  Данные:
	|    Операция: Загрузка
	|
	|Данные:
	|  Формат: JSON
	|
	|Причина:
	|  1: В файле конфигурации обнаружен некорректный JSON
	|  2: fs.file_not_found: Файл не найден
	|     Данные:
	|       Путь: config.json";
	Ожидаем.Что(Представление).Равно(ОжидаемоеПредставление);

КонецПроцедуры

&Тест
Процедура ТестДолжен_ПродолжитьПодробноеПредставлениеПриОшибкеПреобразованияДанных() Экспорт

	// Подготовка
	Ошибка = ФабрикаОшибок.Создать("core.failed", "Ошибка", Новый НепредставимоеЗначение);

	// Действие
	Представление = (Новый ИсследовательОшибки(Ошибка)).ПодробноеПредставление();

	// Проверка
	ОжидаемоеПредставление = "core.failed: Ошибка
	|
	|Данные:
	|  <недоступно>";
	Ожидаем.Что(Представление).Равно(ОжидаемоеПредставление);

КонецПроцедуры

&Тест
Процедура ТестДолжен_ОтметитьУсечениеВПодробномПредставлении() Экспорт

	// Подготовка
	Цепочка = ДлиннаяЦепочка(65);

	// Действие
	Представление = (Новый ИсследовательОшибки(Цепочка.Ошибка)).ПодробноеПредставление();

	// Проверка
	Ожидаем.Что(Представление).Равно(ПредставлениеУсеченнойЦепочки(65));

КонецПроцедуры

&Тест
Процедура ТестДолжен_НеОтметитьЦепочкуИзШестидесятиЧетырехКадровКакУсеченную() Экспорт

	// Подготовка
	Цепочка = ДлиннаяЦепочка(64);

	// Действие
	Представление = (Новый ИсследовательОшибки(Цепочка.Ошибка)).ПодробноеПредставление();

	// Проверка
	Ожидаем.Что(Представление).Равно(ПредставлениеПолнойЦепочки(64));

КонецПроцедуры

&Тест
Процедура ТестДолжен_ВывестиПодсказкуТолькоВПодробномПредставлении() Экспорт

	// Подготовка
	Ошибка = КонструкторыОшибок.Создать(
		"http.proxy_error",
		"Ошибка прокси",
		Неопределено
	)
		.Подсказка("Проверьте настройки прокси")
		.Построить();

	// Действие
	КраткоеПредставление = (Новый ИсследовательОшибки(Ошибка)).КраткоеПредставление();
	ПодробноеПредставление = (Новый ИсследовательОшибки(Ошибка)).ПодробноеПредставление();

	// Проверка
	Ожидаем.Что(КраткоеПредставление).Равно("http.proxy_error: Ошибка прокси");
	ОжидаемоеПодробноеПредставление = "http.proxy_error: Ошибка прокси
	|
	|Подсказка:
	|  Проверьте настройки прокси";
	Ожидаем.Что(ПодробноеПредставление).Равно(ОжидаемоеПодробноеПредставление);

КонецПроцедуры

&Тест
Процедура ТестДолжен_ВывестиURLПодПодсказкойТолькоВПодробномПредставлении() Экспорт

	// Подготовка
	Ошибка = КонструкторыОшибок.Создать("config.invalid", "Ошибка конфигурации")
		.Подсказка("Проверьте формат файла")
		.URL("https://example.org/errors/config-invalid")
		.Построить();

	// Действие
	Исследователь = Новый ИсследовательОшибки(Ошибка);
	КраткоеПредставление = Исследователь.КраткоеПредставление();
	ПодробноеПредставление = Исследователь.ПодробноеПредставление();

	// Проверка
	Ожидаем.Что(КраткоеПредставление).Равно("config.invalid: Ошибка конфигурации");
	ОжидаемоеПредставление = "config.invalid: Ошибка конфигурации
	|
	|Подсказка:
	|  Проверьте формат файла
	|
	|URL:
	|  https://example.org/errors/config-invalid";
	Ожидаем.Что(ПодробноеПредставление).Равно(ОжидаемоеПредставление);

КонецПроцедуры

&Тест
Процедура ТестДолжен_НачатьПодробноеПредставлениеСВнешнейОбертки() Экспорт

	// Подготовка
	Причина = ФабрикаОшибок.Создать("config.load_failed", "Не удалось загрузить настройки");
	Ошибка = ПомощникТестированияОшибок.ВнешняяОшибка(
		"Не удалось инициализировать приложение",
		Причина
	);

	// Действие
	Краткое = (Новый ИсследовательОшибки(Ошибка)).КраткоеПредставление();
	Подробное = (Новый ИсследовательОшибки(Ошибка)).ПодробноеПредставление();

	// Проверка
	Ожидаем.Что(Краткое).Равно("config.load_failed: Не удалось загрузить настройки");
	Ожидаем.Что(Подробное).Равно("Не удалось инициализировать приложение
	|
	|Причина:
	|  config.load_failed: Не удалось загрузить настройки");

КонецПроцедуры

&Тест
Процедура ТестДолжен_СохранитьКонтекстыБезПросмотреннойПричины() Экспорт

	// Подготовка
	Ошибка = ФабрикаОшибок.Создать("core.failed", "Непросмотренная причина");
	Для Номер = 1 По 64 Цикл
		Ошибка = ФабрикаОшибок.СКонтекстом(Ошибка, "Контекст " + Строка(Номер));
	КонецЦикла;

	// Действие
	Представление = (Новый ИсследовательОшибки(Ошибка)).ПодробноеПредставление();

	// Проверка
	Ожидаем.Что(Представление).Равно(ПредставлениеКонтекстовБезПричины());

КонецПроцедуры

&Тест
Процедура ТестДолжен_ВернутьПервыйКонтекстЕслиКлассификацияЗаГраницейПросмотра() Экспорт

	// Подготовка
	Ошибка = ФабрикаОшибок.Создать("core.failed", "Непросмотренная причина");
	Для Номер = 1 По 64 Цикл
		Ошибка = ФабрикаОшибок.СКонтекстом(Ошибка, "Контекст " + Строка(Номер));
	КонецЦикла;

	// Действие
	Представление = (Новый ИсследовательОшибки(Ошибка)).КраткоеПредставление();

	// Проверка
	Ожидаем.Что(Представление).Равно("Контекст 64");

КонецПроцедуры

&Тест
Процедура ТестДолжен_ЯвноПредставитьПустоеСообщение() Экспорт

	// Подготовка
	Ошибка = ПомощникТестированияОшибок.ВнешняяОшибка("");

	// Действие
	Представление = (Новый ИсследовательОшибки(Ошибка)).ПодробноеПредставление();

	// Проверка
	Ожидаем.Что(Представление).Равно(Символ(34) + Символ(34));

КонецПроцедуры

&Тест
Процедура ТестДолжен_ПредставитьНесколькоКонтекстовВФизическомПорядке() Экспорт

	// Подготовка
	Ошибка = ФабрикаОшибок.Создать("core.failed", "Операция завершилась ошибкой");
	Ошибка = ФабрикаОшибок.СКонтекстом(
		Ошибка,
		"Чтение файла",
		Новый Структура("Путь", "settings.json")
	);
	Ошибка = ФабрикаОшибок.СКонтекстом(
		Ошибка,
		"Загрузка настроек",
		Новый Структура("Попытка", 2)
	);

	// Действие
	Краткое = (Новый ИсследовательОшибки(Ошибка)).КраткоеПредставление();
	Подробное = (Новый ИсследовательОшибки(Ошибка)).ПодробноеПредставление();

	// Проверка
	Ожидаем.Что(Краткое).Равно(
		"core.failed: Загрузка настроек -> Чтение файла -> Операция завершилась ошибкой"
	);
	Ожидаем.Что(Подробное).Равно("core.failed: Операция завершилась ошибкой
	|
	|Контекст:
	|  - Загрузка настроек
	|    Данные:
	|      Попытка: 2
	|  - Чтение файла
	|    Данные:
	|      Путь: settings.json");

КонецПроцедуры

&Тест
Процедура ТестДолжен_ПредставитьМинимальнуюКлассифицированнуюОшибкуОднойСтрокой() Экспорт

	// Подготовка
	Ошибка = ФабрикаОшибок.Создать("core.failed", "Сбой");

	// Действие
	Представление = (Новый ИсследовательОшибки(Ошибка)).ПодробноеПредставление();

	// Проверка
	Ожидаем.Что(Представление).Равно("core.failed: Сбой");

КонецПроцедуры

&Тест
Процедура ТестДолжен_ВыровнятьМногострочныеПоляПодробногоПредставления() Экспорт

	// Подготовка
	Данные = Новый Структура("Текст", "Альфа" + Символы.ПС + "Бета");
	Ошибка = КонструкторыОшибок.Создать(
		"core.multiline",
		"Первая строка" + Символы.ПС + "Вторая строка",
		Данные
	)
		.Подсказка("Первый шаг" + Символы.ПС + "Второй шаг")
		.Построить();

	// Действие
	Представление = (Новый ИсследовательОшибки(Ошибка)).ПодробноеПредставление();

	// Проверка
	ОжидаемоеПредставление = "core.multiline: Первая строка" + Символы.ПС
		+ Пробелы(16) + "Вторая строка" + Символы.ПС
		+ Символы.ПС
		+ "Данные:" + Символы.ПС
		+ "  Текст: Альфа" + Символы.ПС
		+ "         Бета" + Символы.ПС + Символы.ПС
		+ "Подсказка:" + Символы.ПС
		+ "  Первый шаг" + Символы.ПС
		+ "  Второй шаг";
	Ожидаем.Что(Представление).Равно(ОжидаемоеПредставление);

КонецПроцедуры

&Тест
Процедура ТестДолжен_ПредставитьФиксированноеСоответствиеВДанныхОшибки() Экспорт

	// Подготовка
	Соответствие = Новый Соответствие;
	Соответствие.Вставить("Формат", "JSON");
	Соответствие.Вставить("Попытки", 3);
	Данные = Новый ФиксированноеСоответствие(Соответствие);
	Ошибка = ФабрикаОшибок.Создать("config.failed", "Ошибка конфигурации", Данные);

	// Действие
	Представление = (Новый ИсследовательОшибки(Ошибка)).ПодробноеПредставление();

	// Проверка
	Ожидаем.Что(Представление).Равно("config.failed: Ошибка конфигурации
	|
	|Данные:
	|  Попытки: 3
	|  Формат: JSON");

КонецПроцедуры

&Тест
Процедура ТестДолжен_ПредставитьМассивКонтейнеровВДанныхОшибки() Экспорт

	// Подготовка
	Данные = Новый Массив;
	Данные.Добавить(Новый Структура("Идентификатор", 1));
	Данные.Добавить(Новый Массив);
	Ошибка = ФабрикаОшибок.Создать("batch.failed", "Ошибка пакета", Данные);

	// Действие
	Представление = (Новый ИсследовательОшибки(Ошибка)).ПодробноеПредставление();

	// Проверка
	Ожидаем.Что(Представление).Равно("batch.failed: Ошибка пакета
	|
	|Данные:
	|  -
	|    Идентификатор: 1
	|  - []");

КонецПроцедуры

&Тест
Процедура ТестДолжен_ПредставитьПустыеКорневыеКонтейнерыВДанныхОшибки() Экспорт

	// Подготовка
	ПустоеСоответствие = Новый Соответствие;
	Ошибки = Новый Массив;
	Ошибки.Добавить(ФабрикаОшибок.Создать("data.empty", "Пустая структура", Новый Структура));
	Ошибки.Добавить(ФабрикаОшибок.Создать("data.empty", "Пустой массив", Новый Массив));
	Ошибки.Добавить(ФабрикаОшибок.Создать("data.empty", "Пустое соответствие", ПустоеСоответствие));

	// Действие
	Представления = Новый Массив;
	Для Каждого Ошибка Из Ошибки Цикл
		Представления.Добавить((Новый ИсследовательОшибки(Ошибка)).ПодробноеПредставление());
	КонецЦикла;

	// Проверка
	Ожидаем.Что(Представления[0]).Равно(
		"data.empty: Пустая структура" + Символы.ПС + Символы.ПС + "Данные: {}"
	);
	Ожидаем.Что(Представления[1]).Равно(
		"data.empty: Пустой массив" + Символы.ПС + Символы.ПС + "Данные: []"
	);
	Ожидаем.Что(Представления[2]).Равно(
		"data.empty: Пустое соответствие" + Символы.ПС + Символы.ПС + "Данные: {}"
	);

КонецПроцедуры

&Тест
Процедура ТестДолжен_ПредставитьПримитивноеЗначениеДанных() Экспорт

	// Подготовка
	Ошибка = ФабрикаОшибок.Создать("data.invalid", "Недопустимое значение", 42);

	// Действие
	Представление = (Новый ИсследовательОшибки(Ошибка)).ПодробноеПредставление();

	// Проверка
	Ожидаем.Что(Представление).Равно("data.invalid: Недопустимое значение
	|
	|Данные:
	|  42");

КонецПроцедуры

&Тест
Процедура ТестДолжен_ПредставитьКлассифицированнуюПричинуСПараметрами() Экспорт

	// Подготовка
	Причина = КонструкторыОшибок.Создать(
		"db.timeout",
		"Превышено время ожидания",
		Новый Структура("Секунды", 30)
	)
		.Подсказка("Повторите запрос")
		.Построить();
	Ошибка = ФабрикаОшибок.Обернуть(
		Причина,
		"profile.load_failed",
		"Не удалось загрузить профиль",
		Новый Структура("Пользователь", 42)
	);

	// Действие
	Представление = (Новый ИсследовательОшибки(Ошибка)).ПодробноеПредставление();

	// Проверка
	Ожидаем.Что(Представление).Равно("profile.load_failed: Не удалось загрузить профиль
	|
	|Данные:
	|  Пользователь: 42
	|
	|Причина:
	|  db.timeout: Превышено время ожидания
	|    Данные:
	|      Секунды: 30
	|
	|Подсказка:
	|  Повторите запрос");

КонецПроцедуры

&Тест
Процедура ТестДолжен_ВывестиМестоИсходнуюСтрокуИСтекРеальнойОшибки() Экспорт

	// Подготовка
	Цепочка = ПомощникТестированияОшибок.ЦепочкаСРазнымиМестамиСоздания();
	ВнешнийКадр = Новый СнимокКадраОшибки(Цепочка.ВнешнийКадр);
	ВнутреннийКадр = Новый СнимокКадраОшибки(Цепочка.ВнутреннийКадр);

	// Действие
	Представление = (Новый ИсследовательОшибки(Цепочка.Ошибка)).ПодробноеПредставление();

	// Проверка
	ОжидаемоеПредставление = ПредставлениеРеальнойОшибки(ВнешнийКадр, ВнутреннийКадр);
	Ожидаем.Что(Представление).Равно(ОжидаемоеПредставление);

КонецПроцедуры

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

Функция СмешаннаяЦепочка()

	ДанныеВнутреннейОшибки = Новый Структура("Путь", "config.json");
	ВнутренняяОшибка = ФабрикаОшибок.Создать("fs.file_not_found", "Файл не найден", ДанныеВнутреннейОшибки);
	ВнешняяОшибка = ПомощникТестированияОшибок.ВнешняяОшибка(
		"В файле конфигурации обнаружен некорректный JSON",
		ВнутренняяОшибка
	);
	ЭффективнаяОшибка = ФабрикаОшибок.Обернуть(
		ВнешняяОшибка,
		"config.load_failed",
		"Не удалось загрузить конфигурацию",
		Новый Структура("Формат", "JSON")
	);
	Контекст = ФабрикаОшибок.СКонтекстом(
		ЭффективнаяОшибка,
		"Загрузка конфигурации",
		Новый Структура("Операция", "Загрузка")
	);

	Возврат Новый Структура("Ошибка", Контекст);

КонецФункции

Функция ДлиннаяЦепочка(Длина)

	Ошибка = ПомощникТестированияОшибок.ВнешняяОшибка("Внешний кадр 1");

	Для НомерКадра = 2 По Длина Цикл
		Ошибка = ПомощникТестированияОшибок.ВнешняяОшибка(
			СтрШаблон("Внешний кадр %1", НомерКадра),
			Ошибка
		);
	КонецЦикла;

	Возврат Новый Структура("Ошибка", Ошибка);

КонецФункции

Функция ПредставлениеУсеченнойЦепочки(Длина)

	Причины = Новый Массив;
	Для НомерПричины = 1 По 63 Цикл
		НомерВнешнегоКадра = Длина - НомерПричины;
		Причины.Добавить(
			"  " + Строка(НомерПричины) + ": Внешний кадр " + Строка(НомерВнешнегоКадра)
		);
	КонецЦикла;

	Возврат "Внешний кадр " + Строка(Длина) + Символы.ПС + Символы.ПС
		+ "Причина:" + Символы.ПС
		+ СоединитьСтроки(Причины, Символы.ПС) + Символы.ПС + Символы.ПС
		+ "Цепочка причин усечена: показано 64 кадра";

КонецФункции

Функция ПредставлениеПолнойЦепочки(Длина)

	Причины = Новый Массив;
	Для НомерПричины = 1 По Длина - 1 Цикл
		НомерВнешнегоКадра = Длина - НомерПричины;
		Причины.Добавить(
			"  " + Строка(НомерПричины) + ": Внешний кадр " + Строка(НомерВнешнегоКадра)
		);
	КонецЦикла;

	Возврат "Внешний кадр " + Строка(Длина) + Символы.ПС + Символы.ПС
		+ "Причина:" + Символы.ПС
		+ СоединитьСтроки(Причины, Символы.ПС);

КонецФункции

Функция ПредставлениеРеальнойОшибки(ВнешнийКадр, ВнутреннийКадр)

	СтрокиСтека = Новый Массив;
	Нумеровать = ВнешнийКадр.Стек().Количество() > 1;
	Для Номер = 0 По ВнешнийКадр.Стек().ВГраница() Цикл
		Маркер = ?(Нумеровать, Строка(Номер + 1) + ": ", "");
		СтрокиСтека.Добавить("  " + Маркер + Строка(ВнешнийКадр.Стек()[Номер]));
	КонецЦикла;

	Возврат "Не удалось загрузить настройки приложения" + Символы.ПС
		+ "Место: " + МестоКадра(ВнешнийКадр) + Символы.ПС
		+ "Исходная строка: " + СокрЛП(ВнешнийКадр.ИсходнаяСтрока()) + Символы.ПС + Символы.ПС
		+ "Причина:" + Символы.ПС
		+ "  Файл settings.json не найден" + Символы.ПС
		+ "    Место: " + МестоКадра(ВнутреннийКадр) + Символы.ПС
		+ "    Исходная строка: " + СокрЛП(ВнутреннийКадр.ИсходнаяСтрока()) + Символы.ПС + Символы.ПС
		+ "Стек вызовов:" + Символы.ПС
		+ СоединитьСтроки(СтрокиСтека, Символы.ПС);

КонецФункции

Функция ПредставлениеКонтекстовБезПричины()

	Строки = Новый Массив;
	Строки.Добавить("Контекст без просмотренной причины:");
	Номер = 64;
	Пока Номер >= 1 Цикл
		Строки.Добавить("  - Контекст " + Строка(Номер));
		Номер = Номер - 1;
	КонецЦикла;
	Строки.Добавить("");
	Строки.Добавить("Цепочка причин усечена: показано 64 кадра");

	Возврат СоединитьСтроки(Строки, Символы.ПС);

КонецФункции

Функция МестоКадра(Снимок)

	Возврат Снимок.ИмяМодуля() + ":" + Строка(Снимок.НомерСтроки());

КонецФункции

Функция СоединитьСтроки(Строки, Разделитель)

	Результат = "";
	Для Каждого ТекущаяСтрока Из Строки Цикл
		Если Не ПустаяСтрока(Результат) Тогда
			Результат = Результат + Разделитель;
		КонецЕсли;
		Результат = Результат + ТекущаяСтрока;
	КонецЦикла;

	Возврат Результат;

КонецФункции

Функция Пробелы(Количество)

	Результат = "";
	Для Номер = 1 По Количество Цикл
		Результат = Результат + " ";
	КонецЦикла;

	Возврат Результат;

КонецФункции

#КонецОбласти
