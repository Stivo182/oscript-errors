#Использовать "../internal"

#Область ПрограммныйИнтерфейс

// Создаёт классифицированную ошибку без причины.
//
// Параметры:
//  ТипОшибки - Строка - Стабильный машинный тип ошибки.
//  Сообщение - Строка - Человекочитаемое описание ошибки.
//  Данные - Произвольный - Связанные с ошибкой данные.
//  Подсказка - Строка - Подсказка по исправлению ошибки.
//
// Возвращаемое значение:
//  ИнформацияОбОшибке - Новый кадр ошибки.
Функция Создать(ТипОшибки, Сообщение, Данные = Неопределено, Подсказка = "") Экспорт

	Возврат Новый КонструкторОшибки(ТипОшибки, Сообщение)
		.ПроизвольныеДанные(Данные)
		.Подсказка(Подсказка)
		.Построить();

КонецФункции

// Добавляет контекст, не изменяя машинную классификацию причины.
//
// Параметры:
//  Причина - ИнформацияОбОшибке - Непосредственная причина нового кадра.
//  Сообщение - Строка - Описание выполнявшейся операции.
//  Данные - Произвольный - Связанные с контекстом данные.
//
// Возвращаемое значение:
//  ИнформацияОбОшибке - Новый кадр контекста.
Функция СКонтекстом(Причина, Сообщение, Данные = Неопределено) Экспорт

	Возврат Новый КонструкторОшибки(Неопределено, Сообщение, ВидыКадровОшибок.Контекст)
		.ПроизвольныеДанные(Данные)
		.Причина(Причина)
		.Построить();

КонецФункции

// Оборачивает причину новой машинной классификацией.
//
// Параметры:
//  Причина - ИнформацияОбОшибке - Непосредственная причина нового кадра.
//  ТипОшибки - Строка - Новый стабильный машинный тип ошибки.
//  Сообщение - Строка - Человекочитаемое описание ошибки.
//  Данные - Произвольный - Связанные с ошибкой данные.
//  Подсказка - Строка - Подсказка по исправлению ошибки.
//
// Возвращаемое значение:
//  ИнформацияОбОшибке - Новый классифицированный кадр.
Функция Обернуть(Причина, ТипОшибки, Сообщение, Данные = Неопределено, Подсказка = "") Экспорт

	Возврат Новый КонструкторОшибки(ТипОшибки, Сообщение)
		.ПроизвольныеДанные(Данные)
		.Причина(Причина)
		.Подсказка(Подсказка)
		.Построить();

КонецФункции

// Возвращает fluent-конструктор классифицированной ошибки.
//
// Параметры:
//  ТипОшибки - Строка - Стабильный машинный тип ошибки.
//  Сообщение - Строка - Человекочитаемое описание ошибки.
//
// Возвращаемое значение:
//  КонструкторОшибки - Новый конструктор ошибки.
Функция Конструктор(ТипОшибки, Сообщение) Экспорт

	Возврат Новый КонструкторОшибки(ТипОшибки, Сообщение);

КонецФункции

// Создаёт ошибку неверного типа аргумента.
//
// Параметры:
//  ИмяАргумента - Строка - Имя аргумента с неверным типом.
//  ОжидаемыйТип - Строка - Строковое представление ожидаемого типа.
//  ФактическоеЗначение - Произвольный - Значение с неверным типом.
//  Сообщение - Строка - Человекочитаемое описание ошибки.
//
// Возвращаемое значение:
//  ИнформацияОбОшибке - Новый кадр ошибки контракта.
Функция НеверныйТипАргумента(ИмяАргумента, ОжидаемыйТип, ФактическоеЗначение, Сообщение = "") Экспорт

	ПроверкаАргументов.ПроверитьТип("ИмяАргумента", ИмяАргумента, Тип("Строка"));
	ПроверкаАргументов.ПроверитьЗаполненность("ИмяАргумента", ИмяАргумента);
	ПроверкаАргументов.ПроверитьТип("ОжидаемыйТип", ОжидаемыйТип, Тип("Строка"));
	ПроверкаАргументов.ПроверитьЗаполненность("ОжидаемыйТип", ОжидаемыйТип);
	ПроверкаАргументов.ПроверитьТип("Сообщение", Сообщение, Тип("Строка"));

	Возврат ФабрикаСтандартныхОшибок.НеверныйТипАргумента(
		ИмяАргумента,
		ОжидаемыйТип,
		ФактическоеЗначение,
		Сообщение
	);

КонецФункции

// Создаёт ошибку незаполненного обязательного аргумента.
//
// Параметры:
//  ИмяАргумента - Строка - Имя незаполненного аргумента.
//  Сообщение - Строка - Человекочитаемое описание ошибки.
//
// Возвращаемое значение:
//  ИнформацияОбОшибке - Новый кадр ошибки контракта.
Функция АргументНеЗаполнен(ИмяАргумента, Сообщение = "") Экспорт

	ПроверкаАргументов.ПроверитьТип("ИмяАргумента", ИмяАргумента, Тип("Строка"));
	ПроверкаАргументов.ПроверитьЗаполненность("ИмяАргумента", ИмяАргумента);
	ПроверкаАргументов.ПроверитьТип("Сообщение", Сообщение, Тип("Строка"));

	Возврат ФабрикаСтандартныхОшибок.АргументНеЗаполнен(ИмяАргумента, Сообщение);

КонецФункции

// Создаёт ошибку недопустимого значения аргумента.
//
// Параметры:
//  ИмяАргумента - Строка - Имя аргумента с недопустимым значением.
//  Ожидание - Строка - Описание допустимого значения или ограничения.
//  Сообщение - Строка - Человекочитаемое описание ошибки.
//
// Возвращаемое значение:
//  ИнформацияОбОшибке - Новый кадр ошибки контракта.
Функция НедопустимоеЗначениеАргумента(ИмяАргумента, Ожидание, Сообщение = "") Экспорт

	ПроверкаАргументов.ПроверитьТип("ИмяАргумента", ИмяАргумента, Тип("Строка"));
	ПроверкаАргументов.ПроверитьЗаполненность("ИмяАргумента", ИмяАргумента);
	ПроверкаАргументов.ПроверитьТип("Ожидание", Ожидание, Тип("Строка"));
	ПроверкаАргументов.ПроверитьЗаполненность("Ожидание", Ожидание);
	ПроверкаАргументов.ПроверитьТип("Сообщение", Сообщение, Тип("Строка"));

	Возврат ФабрикаСтандартныхОшибок.НедопустимоеЗначениеАргумента(
		ИмяАргумента,
		Ожидание,
		Сообщение
	);

КонецФункции

// Создаёт ошибку аргумента вне допустимого диапазона.
//
// Параметры:
//  ИмяАргумента - Строка - Имя аргумента вне диапазона.
//  ОжидаемыйДиапазон - Строка - Описание допустимого диапазона.
//  Сообщение - Строка - Человекочитаемое описание ошибки.
//
// Возвращаемое значение:
//  ИнформацияОбОшибке - Новый кадр ошибки контракта.
Функция АргументВнеДиапазона(ИмяАргумента, ОжидаемыйДиапазон, Сообщение = "") Экспорт

	ПроверкаАргументов.ПроверитьТип("ИмяАргумента", ИмяАргумента, Тип("Строка"));
	ПроверкаАргументов.ПроверитьЗаполненность("ИмяАргумента", ИмяАргумента);
	ПроверкаАргументов.ПроверитьТип("ОжидаемыйДиапазон", ОжидаемыйДиапазон, Тип("Строка"));
	ПроверкаАргументов.ПроверитьЗаполненность("ОжидаемыйДиапазон", ОжидаемыйДиапазон);
	ПроверкаАргументов.ПроверитьТип("Сообщение", Сообщение, Тип("Строка"));

	Возврат ФабрикаСтандартныхОшибок.АргументВнеДиапазона(
		ИмяАргумента,
		ОжидаемыйДиапазон,
		Сообщение
	);

КонецФункции

// Создаёт ошибку отменённой операции.
//
// Параметры:
//  ИмяОперации - Строка - Имя отменённой операции.
//  Сообщение - Строка - Человекочитаемое описание ошибки.
//
// Возвращаемое значение:
//  ИнформацияОбОшибке - Новый кадр ошибки операции.
Функция ОперацияОтменена(ИмяОперации, Сообщение = "") Экспорт

	ПроверитьСтандартныйПараметр("ИмяОперации", ИмяОперации, Сообщение);
	Возврат ФабрикаСтандартныхОшибок.ОперацияОтменена(ИмяОперации, Сообщение);

КонецФункции

// Создаёт ошибку операции, недопустимой в текущем состоянии.
//
// Параметры:
//  ИмяОперации - Строка - Имя недопустимой операции.
//  Сообщение - Строка - Человекочитаемое описание ошибки.
//
// Возвращаемое значение:
//  ИнформацияОбОшибке - Новый кадр ошибки операции.
Функция ОперацияВНедопустимомСостоянии(ИмяОперации, Сообщение = "") Экспорт

	ПроверитьСтандартныйПараметр("ИмяОперации", ИмяОперации, Сообщение);
	Возврат ФабрикаСтандартныхОшибок.ОперацияВНедопустимомСостоянии(ИмяОперации, Сообщение);

КонецФункции

// Создаёт ошибку нереализованной операции.
//
// Параметры:
//  ИмяОперации - Строка - Имя нереализованной операции.
//  Сообщение - Строка - Человекочитаемое описание ошибки.
//
// Возвращаемое значение:
//  ИнформацияОбОшибке - Новый кадр ошибки операции.
Функция ОперацияНеРеализована(ИмяОперации, Сообщение = "") Экспорт

	ПроверитьСтандартныйПараметр("ИмяОперации", ИмяОперации, Сообщение);
	Возврат ФабрикаСтандартныхОшибок.ОперацияНеРеализована(ИмяОперации, Сообщение);

КонецФункции

// Создаёт ошибку неподдерживаемой операции.
//
// Параметры:
//  ИмяОперации - Строка - Имя неподдерживаемой операции.
//  Сообщение - Строка - Человекочитаемое описание ошибки.
//
// Возвращаемое значение:
//  ИнформацияОбОшибке - Новый кадр ошибки операции.
Функция ОперацияНеПоддерживается(ИмяОперации, Сообщение = "") Экспорт

	ПроверитьСтандартныйПараметр("ИмяОперации", ИмяОперации, Сообщение);
	Возврат ФабрикаСтандартныхОшибок.ОперацияНеПоддерживается(ИмяОперации, Сообщение);

КонецФункции

// Создаёт ошибку превышения времени ожидания операции.
//
// Параметры:
//  ИмяОперации - Строка - Имя операции с превышенным временем ожидания.
//  Сообщение - Строка - Человекочитаемое описание ошибки.
//
// Возвращаемое значение:
//  ИнформацияОбОшибке - Новый кадр ошибки операции.
Функция ИстеклоВремяОжиданияОперации(ИмяОперации, Сообщение = "") Экспорт

	ПроверитьСтандартныйПараметр("ИмяОперации", ИмяОперации, Сообщение);
	Возврат ФабрикаСтандартныхОшибок.ИстеклоВремяОжиданияОперации(ИмяОперации, Сообщение);

КонецФункции

// Создаёт ошибку недоступной операции.
//
// Параметры:
//  ИмяОперации - Строка - Имя недоступной операции.
//  Сообщение - Строка - Человекочитаемое описание ошибки.
//
// Возвращаемое значение:
//  ИнформацияОбОшибке - Новый кадр ошибки операции.
Функция ОперацияНедоступна(ИмяОперации, Сообщение = "") Экспорт

	ПроверитьСтандартныйПараметр("ИмяОперации", ИмяОперации, Сообщение);
	Возврат ФабрикаСтандартныхОшибок.ОперацияНедоступна(ИмяОперации, Сообщение);

КонецФункции

// Создаёт ошибку запрещённого доступа.
//
// Параметры:
//  ИмяРесурса - Строка - Безопасное диагностическое имя ресурса.
//  Сообщение - Строка - Человекочитаемое описание ошибки.
//
// Возвращаемое значение:
//  ИнформацияОбОшибке - Новый кадр ошибки доступа.
Функция ДоступЗапрещен(ИмяРесурса, Сообщение = "") Экспорт

	ПроверитьСтандартныйПараметр("ИмяРесурса", ИмяРесурса, Сообщение);
	Возврат ФабрикаСтандартныхОшибок.ДоступЗапрещен(ИмяРесурса, Сообщение);

КонецФункции

// Создаёт ошибку отсутствующего ресурса.
//
// Параметры:
//  ИмяРесурса - Строка - Безопасное диагностическое имя ресурса.
//  Сообщение - Строка - Человекочитаемое описание ошибки.
//
// Возвращаемое значение:
//  ИнформацияОбОшибке - Новый кадр ошибки ресурса.
Функция РесурсНеНайден(ИмяРесурса, Сообщение = "") Экспорт

	ПроверитьСтандартныйПараметр("ИмяРесурса", ИмяРесурса, Сообщение);
	Возврат ФабрикаСтандартныхОшибок.РесурсНеНайден(ИмяРесурса, Сообщение);

КонецФункции

// Создаёт ошибку уже существующего ресурса.
//
// Параметры:
//  ИмяРесурса - Строка - Безопасное диагностическое имя ресурса.
//  Сообщение - Строка - Человекочитаемое описание ошибки.
//
// Возвращаемое значение:
//  ИнформацияОбОшибке - Новый кадр ошибки ресурса.
Функция РесурсУжеСуществует(ИмяРесурса, Сообщение = "") Экспорт

	ПроверитьСтандартныйПараметр("ИмяРесурса", ИмяРесурса, Сообщение);
	Возврат ФабрикаСтандартныхОшибок.РесурсУжеСуществует(ИмяРесурса, Сообщение);

КонецФункции

// Создаёт ошибку уже используемого ресурса.
//
// Параметры:
//  ИмяРесурса - Строка - Безопасное диагностическое имя ресурса.
//  Сообщение - Строка - Человекочитаемое описание ошибки.
//
// Возвращаемое значение:
//  ИнформацияОбОшибке - Новый кадр ошибки ресурса.
Функция РесурсУжеИспользуется(ИмяРесурса, Сообщение = "") Экспорт

	ПроверитьСтандартныйПараметр("ИмяРесурса", ИмяРесурса, Сообщение);
	Возврат ФабрикаСтандартныхОшибок.РесурсУжеИспользуется(ИмяРесурса, Сообщение);

КонецФункции

// Создаёт ошибку закрытого ресурса.
//
// Параметры:
//  ИмяРесурса - Строка - Безопасное диагностическое имя ресурса.
//  Сообщение - Строка - Человекочитаемое описание ошибки.
//
// Возвращаемое значение:
//  ИнформацияОбОшибке - Новый кадр ошибки ресурса.
Функция РесурсЗакрыт(ИмяРесурса, Сообщение = "") Экспорт

	ПроверитьСтандартныйПараметр("ИмяРесурса", ИмяРесурса, Сообщение);
	Возврат ФабрикаСтандартныхОшибок.РесурсЗакрыт(ИмяРесурса, Сообщение);

КонецФункции

// Создаёт ошибку исчерпанного ресурса.
//
// Параметры:
//  ИмяРесурса - Строка - Безопасное диагностическое имя ресурса.
//  Сообщение - Строка - Человекочитаемое описание ошибки.
//
// Возвращаемое значение:
//  ИнформацияОбОшибке - Новый кадр ошибки ресурса.
Функция РесурсИсчерпан(ИмяРесурса, Сообщение = "") Экспорт

	ПроверитьСтандартныйПараметр("ИмяРесурса", ИмяРесурса, Сообщение);
	Возврат ФабрикаСтандартныхОшибок.РесурсИсчерпан(ИмяРесурса, Сообщение);

КонецФункции

// Создаёт ошибку непустого ресурса.
//
// Параметры:
//  ИмяРесурса - Строка - Безопасное диагностическое имя ресурса.
//  Сообщение - Строка - Человекочитаемое описание ошибки.
//
// Возвращаемое значение:
//  ИнформацияОбОшибке - Новый кадр ошибки ресурса.
Функция РесурсНеПуст(ИмяРесурса, Сообщение = "") Экспорт

	ПроверитьСтандартныйПараметр("ИмяРесурса", ИмяРесурса, Сообщение);
	Возврат ФабрикаСтандартныхОшибок.РесурсНеПуст(ИмяРесурса, Сообщение);

КонецФункции

// Создаёт ошибку недоступной возможности.
//
// Параметры:
//  ИмяВозможности - Строка - Имя недоступной возможности.
//  Сообщение - Строка - Человекочитаемое описание ошибки.
//
// Возвращаемое значение:
//  ИнформацияОбОшибке - Новый кадр ошибки возможности.
Функция ВозможностьНедоступна(ИмяВозможности, Сообщение = "") Экспорт

	ПроверитьСтандартныйПараметр("ИмяВозможности", ИмяВозможности, Сообщение);
	Возврат ФабрикаСтандартныхОшибок.ВозможностьНедоступна(ИмяВозможности, Сообщение);

КонецФункции

// Создаёт ошибку отклонённого соединения.
//
// Параметры:
//  Узел - Строка - Безопасное диагностическое имя удалённого узла.
//  Сообщение - Строка - Человекочитаемое описание ошибки.
//
// Возвращаемое значение:
//  ИнформацияОбОшибке - Новый кадр ошибки соединения.
Функция СоединениеОтклонено(Узел, Сообщение = "") Экспорт

	ПроверитьСтандартныйПараметр("Узел", Узел, Сообщение);
	Возврат ФабрикаСтандартныхОшибок.СоединениеОтклонено(Узел, Сообщение);

КонецФункции

// Создаёт ошибку сброшенного соединения.
//
// Параметры:
//  Узел - Строка - Безопасное диагностическое имя удалённого узла.
//  Сообщение - Строка - Человекочитаемое описание ошибки.
//
// Возвращаемое значение:
//  ИнформацияОбОшибке - Новый кадр ошибки соединения.
Функция СоединениеСброшено(Узел, Сообщение = "") Экспорт

	ПроверитьСтандартныйПараметр("Узел", Узел, Сообщение);
	Возврат ФабрикаСтандартныхОшибок.СоединениеСброшено(Узел, Сообщение);

КонецФункции

// Создаёт ошибку закрытого соединения.
//
// Параметры:
//  Узел - Строка - Безопасное диагностическое имя удалённого узла.
//  Сообщение - Строка - Человекочитаемое описание ошибки.
//
// Возвращаемое значение:
//  ИнформацияОбОшибке - Новый кадр ошибки соединения.
Функция СоединениеЗакрыто(Узел, Сообщение = "") Экспорт

	ПроверитьСтандартныйПараметр("Узел", Узел, Сообщение);
	Возврат ФабрикаСтандартныхОшибок.СоединениеЗакрыто(Узел, Сообщение);

КонецФункции

// Создаёт ошибку превышения времени ожидания соединения.
//
// Параметры:
//  Узел - Строка - Безопасное диагностическое имя удалённого узла.
//  Сообщение - Строка - Человекочитаемое описание ошибки.
//
// Возвращаемое значение:
//  ИнформацияОбОшибке - Новый кадр ошибки соединения.
Функция ИстеклоВремяОжиданияСоединения(Узел, Сообщение = "") Экспорт

	ПроверитьСтандартныйПараметр("Узел", Узел, Сообщение);
	Возврат ФабрикаСтандартныхОшибок.ИстеклоВремяОжиданияСоединения(Узел, Сообщение);

КонецФункции

// Создаёт ошибку отсутствующего сетевого имени.
//
// Параметры:
//  СетевоеИмя - Строка - Безопасное диагностическое сетевое имя.
//  Сообщение - Строка - Человекочитаемое описание ошибки.
//
// Возвращаемое значение:
//  ИнформацияОбОшибке - Новый кадр сетевой ошибки.
Функция СетевоеИмяНеНайдено(СетевоеИмя, Сообщение = "") Экспорт

	ПроверитьСтандартныйПараметр("СетевоеИмя", СетевоеИмя, Сообщение);
	Возврат ФабрикаСтандартныхОшибок.СетевоеИмяНеНайдено(СетевоеИмя, Сообщение);

КонецФункции

// Создаёт ошибку некорректных данных.
//
// Параметры:
//  ИмяДанных - Строка - Безопасное диагностическое имя данных.
//  Сообщение - Строка - Человекочитаемое описание ошибки.
//
// Возвращаемое значение:
//  ИнформацияОбОшибке - Новый кадр ошибки данных.
Функция НекорректныеДанные(ИмяДанных, Сообщение = "") Экспорт

	ПроверитьСтандартныйПараметр("ИмяДанных", ИмяДанных, Сообщение);
	Возврат ФабрикаСтандартныхОшибок.НекорректныеДанные(ИмяДанных, Сообщение);

КонецФункции

// Создаёт ошибку слишком больших данных.
//
// Параметры:
//  ИмяДанных - Строка - Безопасное диагностическое имя данных.
//  Ограничение - Строка - Описание допустимого размера.
//  Сообщение - Строка - Человекочитаемое описание ошибки.
//
// Возвращаемое значение:
//  ИнформацияОбОшибке - Новый кадр ошибки данных.
Функция ДанныеСлишкомБольшие(ИмяДанных, Ограничение, Сообщение = "") Экспорт

	ПроверитьДваСтандартныхПараметра(
		"ИмяДанных",
		ИмяДанных,
		"Ограничение",
		Ограничение,
		Сообщение
	);
	Возврат ФабрикаСтандартныхОшибок.ДанныеСлишкомБольшие(ИмяДанных, Ограничение, Сообщение);

КонецФункции

// Создаёт ошибку повреждённых данных.
//
// Параметры:
//  ИмяДанных - Строка - Безопасное диагностическое имя данных.
//  Сообщение - Строка - Человекочитаемое описание ошибки.
//
// Возвращаемое значение:
//  ИнформацияОбОшибке - Новый кадр ошибки данных.
Функция ДанныеПовреждены(ИмяДанных, Сообщение = "") Экспорт

	ПроверитьСтандартныйПараметр("ИмяДанных", ИмяДанных, Сообщение);
	Возврат ФабрикаСтандартныхОшибок.ДанныеПовреждены(ИмяДанных, Сообщение);

КонецФункции

// Создаёт ошибку несоответствия данных заявленной кодировке.
//
// Параметры:
//  ИмяДанных - Строка - Безопасное диагностическое имя данных.
//  Кодировка - Строка - Имя заявленной кодировки.
//  Сообщение - Строка - Человекочитаемое описание ошибки.
//
// Возвращаемое значение:
//  ИнформацияОбОшибке - Новый кадр ошибки кодировки.
Функция НевернаяКодировкаДанных(ИмяДанных, Кодировка, Сообщение = "") Экспорт

	ПроверитьДваСтандартныхПараметра(
		"ИмяДанных",
		ИмяДанных,
		"Кодировка",
		Кодировка,
		Сообщение
	);
	Возврат ФабрикаСтандартныхОшибок.НевернаяКодировкаДанных(ИмяДанных, Кодировка, Сообщение);

КонецФункции

// Создаёт ошибку неподдерживаемой кодировки.
//
// Параметры:
//  Кодировка - Строка - Имя неподдерживаемой кодировки.
//  Сообщение - Строка - Человекочитаемое описание ошибки.
//
// Возвращаемое значение:
//  ИнформацияОбОшибке - Новый кадр ошибки кодировки.
Функция КодировкаНеПоддерживается(Кодировка, Сообщение = "") Экспорт

	ПроверитьСтандартныйПараметр("Кодировка", Кодировка, Сообщение);
	Возврат ФабрикаСтандартныхОшибок.КодировкаНеПоддерживается(Кодировка, Сообщение);

КонецФункции

// Создаёт ошибку неизвестной кодировки.
//
// Параметры:
//  Кодировка - Строка - Неизвестное имя кодировки.
//  Сообщение - Строка - Человекочитаемое описание ошибки.
//
// Возвращаемое значение:
//  ИнформацияОбОшибке - Новый кадр ошибки кодировки.
Функция НеизвестнаяКодировка(Кодировка, Сообщение = "") Экспорт

	ПроверитьСтандартныйПараметр("Кодировка", Кодировка, Сообщение);
	Возврат ФабрикаСтандартныхОшибок.НеизвестнаяКодировка(Кодировка, Сообщение);

КонецФункции

// Создаёт ошибку нарушения внутреннего инварианта.
//
// Параметры:
//  Инвариант - Строка - Описание нарушенного предположения программы.
//  Сообщение - Строка - Человекочитаемое описание ошибки.
//
// Возвращаемое значение:
//  ИнформацияОбОшибке - Новый кадр внутренней ошибки.
Функция НарушенВнутреннийИнвариант(Инвариант, Сообщение = "") Экспорт

	ПроверитьСтандартныйПараметр("Инвариант", Инвариант, Сообщение);
	Возврат ФабрикаСтандартныхОшибок.НарушенВнутреннийИнвариант(Инвариант, Сообщение);

КонецФункции

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

Процедура ПроверитьСтандартныйПараметр(ИмяПараметра, Значение, Сообщение)

	ПроверкаАргументов.ПроверитьТип(ИмяПараметра, Значение, Тип("Строка"));
	ПроверкаАргументов.ПроверитьЗаполненность(ИмяПараметра, Значение);
	ПроверкаАргументов.ПроверитьТип("Сообщение", Сообщение, Тип("Строка"));

КонецПроцедуры

Процедура ПроверитьДваСтандартныхПараметра(
	ИмяПараметра1,
	Значение1,
	ИмяПараметра2,
	Значение2,
	Сообщение
)

	ПроверитьСтандартныйПараметр(ИмяПараметра1, Значение1, Сообщение);
	ПроверкаАргументов.ПроверитьТип(ИмяПараметра2, Значение2, Тип("Строка"));
	ПроверкаАргументов.ПроверитьЗаполненность(ИмяПараметра2, Значение2);

КонецПроцедуры

#КонецОбласти
