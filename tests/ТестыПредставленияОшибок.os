#Использовать asserts
#Использовать "fixtures"
#Использовать "../src"

#Область Тесты

&Тест
Процедура ТестДолжен_ВернутьКраткоеПредставлениеТекущегоКадра() Экспорт

	// Подготовка
	Цепочка = СмешаннаяЦепочка();

	// Действие
	Представление = ДиагностикаОшибок.Разобрать(Цепочка.Ошибка).КраткоеПредставление();

	// Проверка
	Ожидаем.Что(Представление).Равно(
		"config.load_failed: Загрузка конфигурации -> Не удалось загрузить конфигурацию"
	);

КонецПроцедуры

&Тест
Процедура ТестДолжен_ВернутьСообщениеВнешнегоКадраВКраткомПредставлении() Экспорт

	// Подготовка
	Ошибка = ПомощникТестированияОшибок.ВнешняяОшибка(
		"В файле конфигурации обнаружен некорректный JSON"
	);

	// Действие
	Представление = ДиагностикаОшибок.Разобрать(Ошибка).КраткоеПредставление();

	// Проверка
	Ожидаем.Что(Представление).Равно("В файле конфигурации обнаружен некорректный JSON");

КонецПроцедуры

&Тест
Процедура ТестДолжен_ВернутьПустоеСообщениеПервогоВнешнегоКадра() Экспорт

	// Подготовка
	Корень = ПомощникТестированияОшибок.ВнешняяОшибка("Корневая ошибка");
	Ошибка = ПомощникТестированияОшибок.ВнешняяОшибка("", Корень);

	// Действие
	Представление = ДиагностикаОшибок.Разобрать(Ошибка).КраткоеПредставление();

	// Проверка
	Ожидаем.Что(Представление).Равно("");

КонецПроцедуры

&Тест
Процедура ТестДолжен_СохранитьПереводыСтрокВКраткомПредставлении() Экспорт

	// Подготовка
	Ошибка = ФабрикаОшибок.Создать("core.failed", "Первая причина" + Символы.ПС + "Вторая причина");
	Ошибка = ФабрикаОшибок.ОбернутьКонтекстом(
		Ошибка,
		"Первый контекст" + Символы.ПС + "Второй контекст"
	);

	// Действие
	Представление = ДиагностикаОшибок.Разобрать(Ошибка).КраткоеПредставление();

	// Проверка
	Ожидаем.Что(Представление).Равно(
		"core.failed: Первый контекст" + Символы.ПС + "Второй контекст -> Первая причина"
			+ Символы.ПС + "Вторая причина"
	);

КонецПроцедуры

&Тест
Процедура ТестДолжен_ВернутьПервыйКонтекстЦепочкиБезКлассифицированнойОшибки() Экспорт

	// Подготовка
	Причина = ПомощникТестированияОшибок.ВнешняяОшибка("Ошибка сторонней библиотеки");
	Ошибка = ФабрикаОшибок.ОбернутьКонтекстом(Причина, "Запуск приложения");

	// Действие
	Представление = ДиагностикаОшибок.Разобрать(Ошибка).КраткоеПредставление();

	// Проверка
	Ожидаем.Что(Представление).Равно("Запуск приложения");

КонецПроцедуры

&Тест
Процедура ТестДолжен_ВернутьДиагностическоеПредставлениеВсехКадров() Экспорт

	// Подготовка
	Цепочка = СмешаннаяЦепочка();

	// Действие
	Представление = ДиагностикаОшибок.Разобрать(Цепочка.Ошибка).ДиагностическоеПредставление();

	// Проверка
	ОжидаемоеПредставление = "config.load_failed: Не удалось загрузить конфигурацию
	|
	|Контекст:
	|  Загрузка конфигурации
	|  Данные:
	|    Операция: Загрузка
	|
	|Данные:
	|  Формат: JSON
	|
	|Причина:
	|  1: В файле конфигурации обнаружен некорректный JSON
	|  2: fs.file_not_found: Файл не найден
	|     Данные:
	|       Путь: config.json";
	Ожидаем.Что(Представление).Равно(ОжидаемоеПредставление);

КонецПроцедуры

&Тест
Процедура ТестДолжен_ПродолжитьДиагностическоеПредставлениеПриОшибкеПреобразованияДанных() Экспорт

	// Подготовка
	Ошибка = ФабрикаОшибок.Создать("core.failed", "Ошибка", Новый НепредставимоеЗначение);

	// Действие
	Представление = ДиагностикаОшибок.Разобрать(Ошибка).ДиагностическоеПредставление();

	// Проверка
	ОжидаемоеПредставление = "core.failed: Ошибка
	|
	|Данные:
	|  <недоступно>";
	Ожидаем.Что(Представление).Равно(ОжидаемоеПредставление);

КонецПроцедуры

&Тест
Процедура ТестДолжен_ОтметитьУсечениеВДиагностическомПредставлении() Экспорт

	// Подготовка
	Цепочка = ДлиннаяЦепочка(65);

	// Действие
	Представление = ДиагностикаОшибок.Разобрать(Цепочка.Ошибка).ДиагностическоеПредставление();

	// Проверка
	Ожидаем.Что(Представление).Равно(ПредставлениеУсеченнойЦепочки(65));

КонецПроцедуры

&Тест
Процедура ТестДолжен_НеОтметитьЦепочкуИзШестидесятиЧетырехКадровКакУсеченную() Экспорт

	// Подготовка
	Цепочка = ДлиннаяЦепочка(64);

	// Действие
	Представление = ДиагностикаОшибок.Разобрать(Цепочка.Ошибка).ДиагностическоеПредставление();

	// Проверка
	Ожидаем.Что(Представление).Равно(ПредставлениеПолнойЦепочки(64));

КонецПроцедуры

&Тест
Процедура ТестДолжен_ВывестиПодсказкуТолькоВДиагностическомПредставлении() Экспорт

	// Подготовка
	Ошибка = Новый ПостроительОшибки(
		"http.proxy_error",
		"Ошибка прокси"
	)
		.Подсказка("Проверьте настройки прокси")
		.Построить();

	// Действие
	КраткоеПредставление = ДиагностикаОшибок.Разобрать(Ошибка).КраткоеПредставление();
	ДиагностическоеПредставление = ДиагностикаОшибок.Разобрать(Ошибка).ДиагностическоеПредставление();

	// Проверка
	Ожидаем.Что(КраткоеПредставление).Равно("http.proxy_error: Ошибка прокси");
	ОжидаемоеДиагностическоеПредставление = "http.proxy_error: Ошибка прокси
	|
	|Подсказка:
	|  Проверьте настройки прокси";
	Ожидаем.Что(ДиагностическоеПредставление).Равно(ОжидаемоеДиагностическоеПредставление);

КонецПроцедуры

&Тест
Процедура ТестДолжен_ВывестиURLПодПодсказкойТолькоВДиагностическомПредставлении() Экспорт

	// Подготовка
	Ошибка = Новый ПостроительОшибки("config.invalid", "Ошибка конфигурации")
		.Подсказка("Проверьте формат файла")
		.URL("https://example.org/errors/config-invalid")
		.Построить();

	// Действие
	Разбор = Новый РазборОшибки(Ошибка);
	КраткоеПредставление = Разбор.КраткоеПредставление();
	ДиагностическоеПредставление = Разбор.ДиагностическоеПредставление();

	// Проверка
	Ожидаем.Что(КраткоеПредставление).Равно("config.invalid: Ошибка конфигурации");
	ОжидаемоеПредставление = "config.invalid: Ошибка конфигурации
	|
	|Подсказка:
	|  Проверьте формат файла
	|
	|URL:
	|  https://example.org/errors/config-invalid";
	Ожидаем.Что(ДиагностическоеПредставление).Равно(ОжидаемоеПредставление);

КонецПроцедуры

&Тест
Процедура ТестДолжен_ВернутьТриПредставленияДляТекущейВнешнейОбертки() Экспорт

	// Подготовка
	Причина = ФабрикаОшибок.Создать("config.load_failed", "Не удалось загрузить настройки");
	Ошибка = ПомощникТестированияОшибок.ВнешняяОшибка(
		"Не удалось инициализировать приложение",
		Причина
	);
	Разбор = ДиагностикаОшибок.Разобрать(Ошибка);
	ОжидаемоеПредставление = "config.load_failed: Не удалось инициализировать приложение"
		+ " -> Не удалось загрузить настройки";

	// Действие
	Краткое = Разбор.КраткоеПредставление();
	Пользовательское = Разбор.ПользовательскоеПредставление();
	Диагностическое = Разбор.ДиагностическоеПредставление();

	// Проверка
	Ожидаем.Что(Краткое).Равно(ОжидаемоеПредставление);
	Ожидаем.Что(Пользовательское).Равно(ОжидаемоеПредставление);
	Ожидаем.Что(Диагностическое).Равно("Не удалось инициализировать приложение
	|
	|Причина:
	|  config.load_failed: Не удалось загрузить настройки");

КонецПроцедуры

&Тест
Процедура ТестДолжен_СохранитьКонтекстыБезПросмотреннойПричины() Экспорт

	// Подготовка
	Ошибка = ФабрикаОшибок.Создать("core.failed", "Непросмотренная причина");
	Для Номер = 1 По 64 Цикл
		Ошибка = ФабрикаОшибок.ОбернутьКонтекстом(Ошибка, "Контекст " + Строка(Номер));
	КонецЦикла;

	// Действие
	Представление = ДиагностикаОшибок.Разобрать(Ошибка).ДиагностическоеПредставление();

	// Проверка
	Ожидаем.Что(Представление).Равно(ПредставлениеКонтекстовБезПричины());

КонецПроцедуры

&Тест
Процедура ТестДолжен_ВернутьПервыйКонтекстЕслиКлассификацияЗаГраницейПросмотра() Экспорт

	// Подготовка
	Ошибка = ФабрикаОшибок.Создать("core.failed", "Непросмотренная причина");
	Для Номер = 1 По 64 Цикл
		Ошибка = ФабрикаОшибок.ОбернутьКонтекстом(Ошибка, "Контекст " + Строка(Номер));
	КонецЦикла;

	// Действие
	Представление = ДиагностикаОшибок.Разобрать(Ошибка).КраткоеПредставление();

	// Проверка
	Ожидаем.Что(Представление).Равно("Контекст 64");

КонецПроцедуры

&Тест
Процедура ТестДолжен_ЯвноПредставитьПустоеСообщение() Экспорт

	// Подготовка
	Ошибка = ПомощникТестированияОшибок.ВнешняяОшибка("");

	// Действие
	Представление = ДиагностикаОшибок.Разобрать(Ошибка).ДиагностическоеПредставление();

	// Проверка
	Ожидаем.Что(Представление).Равно(Символ(34) + Символ(34));

КонецПроцедуры

&Тест
Процедура ТестДолжен_ПредставитьНесколькоКонтекстовВФизическомПорядке() Экспорт

	// Подготовка
	Ошибка = ФабрикаОшибок.Создать("core.failed", "Операция завершилась ошибкой");
	Ошибка = ФабрикаОшибок.ОбернутьКонтекстом(
		Ошибка,
		"Чтение файла",
		Новый Структура("Путь", "settings.json")
	);
	Ошибка = ФабрикаОшибок.ОбернутьКонтекстом(
		Ошибка,
		"Загрузка настроек",
		Новый Структура("Попытка", 2)
	);

	// Действие
	Краткое = ДиагностикаОшибок.Разобрать(Ошибка).КраткоеПредставление();
	Диагностическое = ДиагностикаОшибок.Разобрать(Ошибка).ДиагностическоеПредставление();

	// Проверка
	Ожидаем.Что(Краткое).Равно("core.failed: Загрузка настроек -> Операция завершилась ошибкой");
	Ожидаем.Что(Диагностическое).Равно("core.failed: Операция завершилась ошибкой
	|
	|Контекст:
	|  - Загрузка настроек
	|    Данные:
	|      Попытка: 2
	|  - Чтение файла
	|    Данные:
	|      Путь: settings.json");

КонецПроцедуры

&Тест
Процедура ТестДолжен_ПредставитьМинимальнуюКлассифицированнуюОшибкуОднойСтрокой() Экспорт

	// Подготовка
	Ошибка = ФабрикаОшибок.Создать("core.failed", "Сбой");

	// Действие
	Представление = ДиагностикаОшибок.Разобрать(Ошибка).ДиагностическоеПредставление();

	// Проверка
	Ожидаем.Что(Представление).Равно("core.failed: Сбой");

КонецПроцедуры








&Тест
Процедура ТестДолжен_ВернутьТолькоСообщениеТекущегоВнешнегоКадра() Экспорт

	// Подготовка
	Цепочка = ПомощникТестированияОшибок.ЦепочкаСРазнымиМестамиСоздания();

	// Действие
	Представление = ДиагностикаОшибок.Разобрать(Цепочка.Ошибка).ПользовательскоеПредставление();

	// Проверка
	Ожидаем.Что(Представление).Равно("Не удалось загрузить настройки приложения");

КонецПроцедуры

&Тест
Процедура ТестДолжен_ВернутьТолькоТекущийКонтекстУсеченнойЦепочки() Экспорт

	// Подготовка
	Ошибка = ФабрикаОшибок.Создать("core.failed", "Непросмотренная причина");
	Для Номер = 1 По 64 Цикл
		Ошибка = ФабрикаОшибок.ОбернутьКонтекстом(Ошибка, "Контекст " + Строка(Номер));
	КонецЦикла;

	// Действие
	Представление = ДиагностикаОшибок.Разобрать(Ошибка).ПользовательскоеПредставление();

	// Проверка
	Ожидаем.Что(Представление).Равно("Контекст 64");

КонецПроцедуры

&Тест
Процедура ТестДолжен_ВернутьПользовательскоеПредставлениеТекущегоКадра() Экспорт

	// Подготовка
	Цепочка = СмешаннаяЦепочка();

	// Действие
	Представление = ДиагностикаОшибок.Разобрать(Цепочка.Ошибка).ПользовательскоеПредставление();

	// Проверка
	Ожидаем.Что(Представление).Равно(
		"config.load_failed: Загрузка конфигурации -> Не удалось загрузить конфигурацию"
	);
	Ожидаем.Что(Представление).ЭтоНе().Содержит("Данные:");
	Ожидаем.Что(Представление).ЭтоНе().Содержит("Стек вызовов:");
	Ожидаем.Что(Представление).ЭтоНе().Содержит("В файле конфигурации обнаружен некорректный JSON");

КонецПроцедуры

&Тест
Процедура ТестДолжен_ВернутьПользовательскоеПредставлениеСПодсказкойИURL() Экспорт

	// Подготовка
	Ошибка = Новый ПостроительОшибки("config.invalid", "Ошибка конфигурации")
		.Подсказка("Проверьте формат файла")
		.URL("https://example.org/errors/config-invalid")
		.Построить();

	// Действие
	Представление = ДиагностикаОшибок.Разобрать(Ошибка).ПользовательскоеПредставление();

	// Проверка
	ОжидаемоеПредставление = "config.invalid: Ошибка конфигурации
	|
	|Подсказка:
	|  Проверьте формат файла
	|
	|URL:
	|  https://example.org/errors/config-invalid";
	Ожидаем.Что(Представление).Равно(ОжидаемоеПредставление);

КонецПроцедуры

&Тест
Процедура ТестДолжен_СохранитьКонтекстЧерезВнешнююОбертку() Экспорт

	// Подготовка
	Причина = ФабрикаОшибок.Создать("service.unavailable", "Сервис недоступен");
	Обертка = ПомощникТестированияОшибок.ВнешняяОшибка("Служебная обёртка", Причина);
	Ошибка = ФабрикаОшибок.ОбернутьКонтекстом(Обертка, "Вызов сервиса");

	// Действие
	Представление = ДиагностикаОшибок.Разобрать(Ошибка).ПользовательскоеПредставление();

	// Проверка
	Ожидаем.Что(Представление).Содержит("Вызов сервиса");

КонецПроцедуры

&Тест
Процедура ТестДолжен_ОпуститьПричинуПодробногоПредставленияПриУсечении() Экспорт

	// Подготовка
	Цепочка = ДлиннаяЦепочка(65);

	// Действие
	Представление = ДиагностикаОшибок.Разобрать(Цепочка.Ошибка).ПользовательскоеПредставление();

	// Проверка
	Ожидаем.Что(Представление).Равно("Внешний кадр 65");
	Ожидаем.Что(Представление).ЭтоНе().Содержит("Причина:");

КонецПроцедуры

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

Функция СмешаннаяЦепочка()

	ДанныеВнутреннейОшибки = Новый Структура("Путь", "config.json");
	ВнутренняяОшибка = ФабрикаОшибок.Создать("fs.file_not_found", "Файл не найден", ДанныеВнутреннейОшибки);
	ВнешняяОшибка = ПомощникТестированияОшибок.ВнешняяОшибка(
		"В файле конфигурации обнаружен некорректный JSON",
		ВнутренняяОшибка
	);
	ЭффективнаяОшибка = ФабрикаОшибок.Обернуть(
		ВнешняяОшибка,
		"config.load_failed",
		"Не удалось загрузить конфигурацию",
		Новый Структура("Формат", "JSON")
	);
	Контекст = ФабрикаОшибок.ОбернутьКонтекстом(
		ЭффективнаяОшибка,
		"Загрузка конфигурации",
		Новый Структура("Операция", "Загрузка")
	);

	Возврат Новый Структура("Ошибка", Контекст);

КонецФункции

Функция ДлиннаяЦепочка(Длина)

	Ошибка = ПомощникТестированияОшибок.ВнешняяОшибка("Внешний кадр 1");

	Для НомерКадра = 2 По Длина Цикл
		Ошибка = ПомощникТестированияОшибок.ВнешняяОшибка(
			СтрШаблон("Внешний кадр %1", НомерКадра),
			Ошибка
		);
	КонецЦикла;

	Возврат Новый Структура("Ошибка", Ошибка);

КонецФункции

Функция ПредставлениеУсеченнойЦепочки(Длина)

	Причины = Новый Массив;
	Для НомерПричины = 1 По 63 Цикл
		НомерВнешнегоКадра = Длина - НомерПричины;
		Причины.Добавить(
			"  " + Строка(НомерПричины) + ": Внешний кадр " + Строка(НомерВнешнегоКадра)
		);
	КонецЦикла;

	Возврат "Внешний кадр " + Строка(Длина) + Символы.ПС + Символы.ПС
		+ "Причина:" + Символы.ПС
		+ СоединитьСтроки(Причины, Символы.ПС) + Символы.ПС + Символы.ПС
		+ "Цепочка причин усечена";

КонецФункции

Функция ПредставлениеПолнойЦепочки(Длина)

	Причины = Новый Массив;
	Для НомерПричины = 1 По Длина - 1 Цикл
		НомерВнешнегоКадра = Длина - НомерПричины;
		Причины.Добавить(
			"  " + Строка(НомерПричины) + ": Внешний кадр " + Строка(НомерВнешнегоКадра)
		);
	КонецЦикла;

	Возврат "Внешний кадр " + Строка(Длина) + Символы.ПС + Символы.ПС
		+ "Причина:" + Символы.ПС
		+ СоединитьСтроки(Причины, Символы.ПС);

КонецФункции


Функция ПредставлениеКонтекстовБезПричины()

	Строки = Новый Массив;
	Строки.Добавить("Контекст без просмотренной причины:");
	Номер = 64;
	Пока Номер >= 1 Цикл
		Строки.Добавить("  - Контекст " + Строка(Номер));
		Номер = Номер - 1;
	КонецЦикла;
	Строки.Добавить("");
	Строки.Добавить("Цепочка причин усечена");

	Возврат СоединитьСтроки(Строки, Символы.ПС);

КонецФункции


Функция СоединитьСтроки(Строки, Разделитель)

	Результат = "";
	Для Каждого ТекущаяСтрока Из Строки Цикл
		Если Не ПустаяСтрока(Результат) Тогда
			Результат = Результат + Разделитель;
		КонецЕсли;
		Результат = Результат + ТекущаяСтрока;
	КонецЦикла;

	Возврат Результат;

КонецФункции


#КонецОбласти
