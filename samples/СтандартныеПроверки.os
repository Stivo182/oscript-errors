// Демонстрирует проверки значений и прямой вызов стандартных ошибок.
// Все ожидаемые ошибки перехватываются и не прерывают выполнение сценария.

#Использовать errors

Процедура СообщитьКодОшибки(Ошибка)

	Снимок = Новый СнимокКадраОшибки(Ошибка);
	Сообщить(Снимок.КодОшибки() + ": " + Снимок.Сообщение());

КонецПроцедуры

Попытка
	ПроверкаАргументов.ПроверитьДиапазон("Порт", 70000, 1, 65535);
Исключение
	СообщитьКодОшибки(ИнформацияОбОшибке());
КонецПопытки;

Попытка
	ВызватьИсключение СтандартныеОшибки.ОперацияВНедопустимомСостоянии("Публикация");
Исключение
	СообщитьКодОшибки(ИнформацияОбОшибке());
КонецПопытки;

Попытка
	ВызватьИсключение СтандартныеОшибки.РесурсНеНайден("Настройки приложения");
Исключение
	СообщитьКодОшибки(ИнформацияОбОшибке());
КонецПопытки;

Попытка
	ВызватьИсключение СтандартныеОшибки.КодировкаНеПоддерживается("UTF-7");
Исключение
	СообщитьКодОшибки(ИнформацияОбОшибке());
КонецПопытки;

Попытка
	ВызватьИсключение СтандартныеОшибки.НарушенВнутреннийИнвариант("Состояние задачи согласовано");
Исключение
	СообщитьКодОшибки(ИнформацияОбОшибке());
КонецПопытки;
